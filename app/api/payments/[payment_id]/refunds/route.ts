import { NextResponse } from "next/server"; import { z } from "zod"; import { RefundCreateInput } from "@/lib/api-contracts"; import { requireAgentCustomer } from "@/lib/auth/agent"; import { bad,failure } from "@/lib/api-utils";
export async function POST(request:Request,{params}:{params:Promise<{payment_id:string}>}){try{const input=RefundCreateInput.parse(await request.json());const {payment_id}=await params;const {customer,supabase}=await requireAgentCustomer(request);const {data:payment,error}=await supabase.from("payments").select("*").eq("id",payment_id).eq("customer_id",customer.id).single();if(error)throw error;const {data,error:re}=await supabase.from("refunds").insert({customer_id:customer.id,payment_id,booking_id:payment.booking_id,amount:input.amount,currency:payment.currency,status:"PENDING",reason:input.reason||null}).select("*").single();if(re)throw re;return NextResponse.json(data,{status:201});}catch(e){return e instanceof z.ZodError?bad(e):failure(e)}}
