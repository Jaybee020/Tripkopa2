import { NextResponse } from "next/server"; import { z } from "zod"; import { FlightSearchInput } from "@/lib/api-contracts"; import { requireAgentCustomer } from "@/lib/auth/agent"; import { taketrips } from "@/lib/services/taketrips"; import { bad,failure } from "@/lib/api-utils";
export async function POST(request:Request){try{const input=FlightSearchInput.parse(await request.json());const {customer,supabase}=await requireAgentCustomer(request);const provider=await taketrips.search({from:input.origin,to:input.destination,departureDate:input.departure_date,returnDate:input.return_date,adult:input.passenger_count,cabinClass:input.cabin_class});const {data,error}=await supabase.from("flight_searches").insert({customer_id:customer.id,origin:input.origin,destination:input.destination,departure_date:input.departure_date,return_date:input.return_date||null,trip_type:input.trip_type,passenger_count:input.passenger_count,cabin_class:input.cabin_class,status:"COMPLETED",results:provider}).select("*").single();if(error)throw error;return NextResponse.json(data,{status:201});}catch(e){return e instanceof z.ZodError?bad(e):failure(e)}}
