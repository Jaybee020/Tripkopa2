import { NextResponse } from "next/server"; import { z } from "zod"; import { PasswordResetRequest } from "@/lib/api-contracts"; import { createServerSupabase } from "@/lib/auth/server"; import { bad, failure } from "@/lib/api-utils";
export async function POST(request: Request) { try { const input=PasswordResetRequest.parse(await request.json()); const supabase=createServerSupabase(); const {error}=await supabase.auth.resetPasswordForEmail(input.email); if(error) throw error; return NextResponse.json({sent:true}); } catch(error) { return error instanceof z.ZodError ? bad(error) : failure(error); } }
