import { NextResponse } from "next/server"; import { z } from "zod"; import { BookingCreateInput } from "@/lib/api-contracts"; import { requireAgentCustomer } from "@/lib/auth/agent"; import { bad,failure } from "@/lib/api-utils";
export async function POST(request:Request){try{const input=BookingCreateInput.parse(await request.json());const {customer,supabase}=await requireAgentCustomer(request);const {data:quote,error:qe}=await supabase.from("quotes").select("*").eq("id",input.quote_id).eq("customer_id",customer.id).single();if(qe)throw qe;const {data,error}=await supabase.from("bookings").insert({customer_id:customer.id,quote_id:quote.id,booking_type:input.booking_type,status:input.terms_accepted?"AWAITING_PAYMENT":"AWAITING_TERMS",currency:quote.currency,total_amount:quote.total_amount,deposit_amount:quote.deposit_amount||null,amount_paid:0,balance_amount:quote.total_amount,rule_version:quote.rule_version,passengers:input.passengers,flight_details:quote.details}).select("*").single();if(error)throw error;return NextResponse.json(data,{status:201});}catch(e){return e instanceof z.ZodError?bad(e):failure(e)}}
